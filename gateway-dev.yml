spring:
  cloud:
    gateway:
      routes:
        - id: wms-service
            predicates:
              - Path=/** # Path predicate for URI path matching
            filters:
              - PrefixPath=/api